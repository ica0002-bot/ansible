---
  - name: Init
    hosts: vm1
    become: yes
    tasks:
      - name: Test connection
        ping:

      - name: Add ssh keys
        authorized_key: 
          user: ubuntu
          key: https://github.com/hudolejev.keys

      - name: Add ssh keys
        authorized_key: 
          user: ubuntu
          key: https://github.com/romankuchin.keys

      - name: Upload github script
        template:
          src: github_bot.j2
          dest: /root/github_bot.py
          mode: 0700
